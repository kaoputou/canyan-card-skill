openapi: 3.1.0
info:
  title: 窄门餐眼·名片 API
  description: >
    管理餐饮行业名片和人脉。查询名片、人脉信息时必须调用对应接口获取实时数据。
    发起名片交换、接受/拒绝请求、创建或编辑名片等写操作，执行前必须先告知用户并确认。
    Token 失效时（code 40301），引导用户回 App 重新生成 Skill Token。
    配额用完时（code 42901），提示用户在 App 内升级会员。
  version: 0.1.0

servers:
  - url: https://fa-dev.kaoputou.com/api/skill

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 在窄门餐眼 App「我的 → 关于我们 → Token 管理」中生成 Skill Token，形如 sk_xxxxxx

  schemas:
    SuccessResponse:
      type: object
      properties:
        code:
          type: integer
          example: 0
        data:
          type: object

    ErrorResponse:
      type: object
      properties:
        code:
          type: integer
          description: >
            40301 Token 无效或已失效 |
            40302 自己尚未创建名片 |
            40303 对方尚未创建名片 |
            40401 资源不存在 |
            40901 状态冲突（已有名片 / 请求已处理）|
            40902 名片审核中不可修改 |
            42201 参数错误 |
            42901 配额已用尽 |
            42902 请求过于频繁
        message:
          type: string
        retry_after:
          type: integer
          description: 限流时返回，单位秒

paths:
  /v1/card/me:
    get:
      operationId: get_my_card
      summary: 查看我的名片
      description: 查看当前用户的名片信息和审核状态。用户询问「我的名片」「审核过了吗」时使用。
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          card_slug:
                            type: string
                            description: 名片唯一标识，仅用于后续接口调用，不要展示给用户
                          name:
                            type: string
                          avatar:
                            type: string
                            description: 头像图片 URL
                          position:
                            type: string
                            description: 职位
                          company:
                            type: string
                          mobile:
                            type: string
                          viewCondition:
                            type: integer
                            description: 查看门槛
                          showStatus:
                            type: integer
                            description: 展示状态
                          authExpiredTime:
                            type: string
                            description: 认证到期日，未生效时为空字符串
                          authFailReason:
                            type: string
                            description: 最近一次审核失败原因，未失败时为空字符串
                          introImages:
                            type: array
                            items:
                              type: string
                            description: 名片介绍图 URL 列表
                          email:
                            type: string
                          address:
                            type: string
                          intro:
                            type: string
                          status:
                            type: string
                            enum: [none, pending, approved, rejected]
                            description: none 未认证 / pending 审核中 / approved 已通过 / rejected 已拒绝

  /v1/card/create:
    post:
      operationId: create_card
      summary: 创建名片
      description: >
        申请创建主名片，提交后进入审核（通常 1 个工作日）。主名片是对外展示和被搜索的唯一名片。
        创建时需通过 target_type 指定身份：品牌方（公司有餐饮门店）、加盟商（加盟餐饮品牌）、
        供应商（为餐饮门店提供食材/耗材/器具/服务）、其他（媒体/商圈招商等）。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, mobile, cover, position, company, intro]
              properties:
                name:
                  type: string
                  maxLength: 32
                mobile:
                  type: string
                  maxLength: 20
                cover:
                  type: string
                  description: 头像图片 URL
                position:
                  type: string
                  maxLength: 32
                  description: 职位
                company:
                  type: string
                  maxLength: 50
                intro:
                  type: string
                  maxLength: 1024
                email:
                  type: string
                  maxLength: 64
                wechat:
                  type: string
                  maxLength: 64
                address:
                  type: string
                  maxLength: 64
                target_type:
                  type: integer
                  enum: [1, 2, 3, 4]
                  description: 身份类型：1 供应商 / 2 品牌方 / 3 服务商（其他）/ 4 加盟商
                target_slug:
                  type: string
                  description: 关联品牌或供应链的唯一标识，仅用于后续接口调用，不要展示给用户
                target_logo:
                  type: string
                  description: 关联品牌 logo URL
                show_status:
                  type: integer
                  description: 是否公开展示名片
                view_condition:
                  type: integer
                  description: 查看名片的条件
      responses:
        '200':
          description: 申请已提交，等待审核
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          card_slug:
                            type: string
                            description: 名片唯一标识，仅用于后续接口调用，不要展示给用户
                          status:
                            type: string
                            example: pending
                          message:
                            type: string
                            description: 固定返回审核提示语，直接展示给用户

  /v1/card/search-target:
    get:
      operationId: search_target
      summary: 搜索可关联企业
      description: >
        创建名片时，按企业名称搜索已收录的品牌方、供应商、服务商或加盟商候选企业。
        找到合适的企业后，将返回结果中的 targetSlug、targetType 传给 create_card 接口。
        此接口只负责查询，不会修改任何名片数据。
      parameters:
        - name: search
          in: query
          required: true
          schema:
            type: string
          description: 企业名称关键词，不能为空
        - name: target_type
          in: query
          required: true
          schema:
            type: integer
            enum: [1, 2, 3, 4]
          description: 企业类型：1 供应商 / 2 品牌方 / 3 服务商 / 4 加盟商（加盟商底层复用品牌搜索）
        - name: page_size
          in: query
          schema:
            type: integer
            default: 8
            maximum: 20
          description: 返回条数，默认 8，最大 20
        - name: full_match
          in: query
          schema:
            type: integer
            enum: [0, 1]
            default: 0
          description: 0 模糊匹配（默认）/ 1 精确匹配
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          records:
                            type: array
                            items:
                              type: object
                              properties:
                                targetType:
                                  type: integer
                                  description: 企业类型，见 target_type 枚举
                                targetSlug:
                                  type: string
                                  description: 企业唯一标识，仅用于 create_card 的 target_slug 字段，不要展示给用户
                                targetName:
                                  type: string
                                  description: 企业名称
                          pageSize:
                            type: integer
                            description: 实际生效的返回上限
                          fullMatch:
                            type: boolean
                            description: 当前查询是否按精确匹配执行

  /v1/card/search:
    get:
      operationId: search_cards
      summary: 搜索名片
      description: 搜索餐饮行业名片。只返回审核通过的主名片，每个用户最多展示一张主名片。
      parameters:
        - name: keyword
          in: query
          required: true
          schema:
            type: string
          description: 搜索关键词，如姓名、公司、职位
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 50
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          records:
                            type: array
                            items:
                              type: object
                              properties:
                                slug:
                                  type: string
                                  description: 名片唯一标识，仅用于后续 exchange 接口的 card_slug 字段，不要展示给用户
                                name:
                                  type: string
                                cover:
                                  type: string
                                  description: 头像图片
                                position:
                                  type: string
                                  description: 职位
                                company:
                                  type: string
                                intro:
                                  type: string
                                  description: 个人简介
                                targetType:
                                  type: integer
                                  description: 身份类型
                                targetName:
                                  type: string
                                  description: 关联品牌/机构名称
                                isAuth:
                                  type: boolean
                                  description: 是否已认证
                                isMine:
                                  type: boolean
                                  description: 是否是自己的名片
                                isZmBro:
                                  type: boolean
                                  description: 是否窄门兄弟
                                tagList:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      label:
                                        type: string
                                      type:
                                        type: string
                                  description: 标签列表
                                latestVisitDay:
                                  type: integer
                                  nullable: true
                                  description: 最近活跃日期
                                sendStatus:
                                  type: integer
                                  description: 0 表示已可查看联系方式（已交换/已收到分享/对方已投递）
                                mobile:
                                  type: string
                                  description: >
                                    手机号。自己的名片、对方无查看门槛、或 sendStatus=0 时返回真实值；否则返回空字符串
                                wechat:
                                  type: string
                                  description: 微信号，可见规则同 mobile
                          hasNext:
                            type: boolean
                            description: 是否有下一页

  /v1/card/exchange:
    post:
      operationId: exchange_card
      summary: 发起名片交换
      description: >
        向指定名片发起交换请求。
        card_slug 从搜索结果的 slug 字段获取。
        配额：已认证品牌方/加盟商/供应商每日 10 次，SVIP 每日 50 次，VIP 每日 1 次，其他用户累计 5 次。
        返回 remaining 表示剩余次数。7 天无回应可再次投递。
        执行前必须先向用户确认。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [card_slug]
              properties:
                card_slug:
                  type: string
                  description: 目标名片唯一标识，从搜索结果的 slug 字段获取，仅用于接口调用，不要展示给用户
                reason:
                  type: string
                  maxLength: 256
                  description: 附言，建议填写交换原因
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          request_id:
                            type: string
                          status:
                            type: string
                            example: pending
                          remaining:
                            type: integer
                            description: 剩余可发起次数

  /v1/card/received:
    get:
      operationId: get_received_cards
      summary: 收到的名片请求
      description: 查看别人发给自己的名片交换请求列表。仅返回待处理（pending）记录。
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 50
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          total:
                            type: integer
                          list:
                            type: array
                            items:
                              type: object
                              properties:
                                request_id:
                                  type: string
                                from_user:
                                  type: object
                                  properties:
                                    card_slug:
                                      type: string
                                      description: 名片唯一标识，仅用于后续接口调用，不要展示给用户
                                    name:
                                      type: string
                                    position:
                                      type: string
                                    company:
                                      type: string
                                    avatar:
                                      type: string
                                      description: 对方头像图片
                                    mobile:
                                      type: string
                                      description: >
                                        对方手机号。收到投递请求时始终返回，无论对方是否公开。
                                    wechat:
                                      type: string
                                      description: 对方微信号，若对方填写则返回
                                    email:
                                      type: string
                                      description: 对方邮箱，若对方填写则返回
                                status:
                                  type: string
                                  enum: [pending, accepted, rejected]
                                reason:
                                  type: string
                                latest_visit_day:
                                  type: string
                                  description: 对方最近活跃日期
                                created_at:
                                  type: string

  /v1/card/delivered:
    get:
      operationId: get_delivered_cards
      summary: 我投递的名片请求
      description: 查看自己发出去的名片交换请求及对方处理结果，含剩余配额 remaining。
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [all, pending, accepted, rejected]
            default: all
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 50
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          total:
                            type: integer
                          remaining:
                            type: integer
                            description: 剩余可发起次数
                          list:
                            type: array
                            items:
                              type: object
                              properties:
                                request_id:
                                  type: string
                                to_user:
                                  type: object
                                  properties:
                                    card_slug:
                                      type: string
                                      description: 名片唯一标识，仅用于后续接口调用，不要展示给用户
                                    name:
                                      type: string
                                    position:
                                      type: string
                                    company:
                                      type: string
                                    avatar:
                                      type: string
                                      description: 对方头像图片
                                    mobile:
                                      type: string
                                      description: >
                                        对方手机号。请求已被接受（status=accepted）时始终返回，无论对方是否公开。
                                        status=pending 或 rejected 时为 null。
                                    wechat:
                                      type: string
                                      description: 对方微信号，status=accepted 时若对方填写则返回
                                    email:
                                      type: string
                                      description: 对方邮箱，status=accepted 时若对方填写则返回
                                status:
                                  type: string
                                  enum: [pending, accepted, rejected]
                                created_at:
                                  type: string

  /v1/card/received/{request_id}/respond:
    post:
      operationId: respond_to_request
      summary: 接受或拒绝名片请求
      description: >
        接受或拒绝收到的名片交换请求。
        request_id 从 get_received_cards 结果中获取。
        接受后双方均可查看对方联系方式。
        执行前必须先向用户确认。
      parameters:
        - name: request_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action:
                  type: string
                  enum: [accept, reject]
                  description: accept 接受 / reject 拒绝
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          request_id:
                            type: string
                          status:
                            type: string
                            enum: [accepted, rejected]

  /v1/card/{slug}/modify:
    post:
      operationId: update_card
      summary: 编辑名片
      description: >
        修改当前用户自己的主名片，只传需要修改的字段即可。
        名片处于审核中（status=pending）时不可修改，需等审核完成。
        名片已认证时，name 和 position 的修改会重新进入审核，其余支持字段直接生效。
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
          description: 当前用户自己的主名片唯一标识，仅用于接口调用，不要展示给用户
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 32
                mobile:
                  type: string
                  maxLength: 20
                cover:
                  type: string
                  description: 头像图片 URL 或七牛 key
                position:
                  type: string
                  maxLength: 32
                  description: 职位
                intro:
                  type: string
                  maxLength: 1024
                email:
                  type: string
                  maxLength: 64
                wechat:
                  type: string
                  maxLength: 64
                address:
                  type: string
                  maxLength: 64
                intro_images:
                  type: string
                  description: 七牛 URL / key 字符串，多个值用 | 分隔
                show_status:
                  type: integer
                  description: 展示状态
                view_condition:
                  type: integer
                  description: 查看门槛
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          card_slug:
                            type: string
                            description: 当前用户主名片的唯一标识，仅用于后续接口调用，不要展示给用户
                          status:
                            type: string
                            enum: [pending, approved, rejected]
                          message:
                            type: string

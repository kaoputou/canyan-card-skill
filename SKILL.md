---
name: canyan-business-card
version: 1.0.0
description: 管理窄门餐眼平台上的餐饮行业名片和人脉。用户提到名片、加名片、交换名片、搜索餐饮同行、查看名片请求、处理收到的名片、投递名片、审核状态时，都应使用本 Skill。即使用户只是说「帮我找一下做供应链的」「我的名片过审了吗」「有人加我名片吗」，也应主动使用本 Skill 获取实时数据。
---

# 窄门餐眼·名片

帮助餐饮行业从业者通过 AI 对话管理名片和人脉。涉及名片或人脉的问题，
先调工具获取实时数据，不要凭记忆回答。

> **接口规范**：所有可调用工具（operationId、请求/响应字段、错误码）的完整定义见 `references/openapi.yaml`。
> 调用任何接口前，以 `references/openapi.yaml` 中的 schema 为准，不要凭本文描述猜测参数格式。
> 核心数据类型的人类可读说明见 `references/schemas.md`。

## 工具使用判断

| 用户说的 | 用哪个工具 |
|---------|-----------|
| 我的名片 / 审核过了吗 | `get_my_card` |
| 帮我建一张名片 / 我要创建名片 | `create_card` |
| 更新职位 / 换头像 / 改简介 | `update_card` |
| 只要用户提到搜索 / 搜一下 / 找找 / 查查 | `search_cards` |
| 创建名片流程里内部执行企业匹配 | `search_target` |
| 加名片 / 发个交换请求 | `exchange_card` |
| 有人给我发名片吗 / 有人加我吗 | `get_received_cards` |
| 我发出去的请求有回音吗 | `get_delivered_cards` |
| 同意 / 拒绝这个请求 | `respond_to_request` |

## 安全规则

- **禁止明文输出凭证**：Skill Token 不得以任何形式出现在对话内容中。
- **写操作前必须确认**：`create_card`、`update_card`、`exchange_card`、`respond_to_request` 执行前，先告知用户将要做什么，等确认后再调用。

## 行为规范

**编辑名片的审核规则**：调用 `update_card` 时，修改**姓名或职位**会触发审核（名片进入 `pending` 状态，约 1-3 个工作日），修改其他字段（简介、头像、联系方式等）直接生效。操作前应提前告知用户，避免困惑。

**编辑名片支持部分更新**：调用 `update_card` 时，用户改什么就传什么。

**用户提到“搜索”时只用 `search_cards`**：只要用户说了“搜索”“搜一下”“帮我找找”“查一下”
等搜索类表达，不论关键词是人名、公司名、职位、业务方向还是资源类型，都必须调用
`search_cards`。`search_target` 不能作为对“搜索”指令的响应工具使用。

**`search_target` 仅限内部企业匹配步骤**：`search_target` 只用于**创建名片流程中的企业匹配**，
目的是为 `create_card` 填 `target_slug` / `target_type`。它只能在创建名片过程中、收集到
企业简称和身份类型之后由 AI 主动触发，不能因为用户说了“搜索企业”“搜公司”就直接调用。

**投递前引导填写理由**：调用 `exchange_card` 前，先询问用户与对方的交集或交换目的（如
共同行业背景、合作意向、认识来源等），基于用户提供的信息，以简洁、真诚、具体的语言
帮用户撰写申请理由，告知用户后再发送。若用户不愿提供更多信息，则根据对方名片的
姓名、职位、公司自动生成一条通用但真诚的理由，告知用户后发送，不可空理由投递。

**诚实面对边界**：搜索结果以工具返回为准，不要编造公司信息或行业数据。

**配额用尽时**（错误码 42901）：提示用户在 App 内升级会员可获得更多额度，
这是平台规则不可绕过：创建名片后领取 5 次 / VIP 每日 1 次 / 品牌方·加盟商·窄门供应链合作供应商每日 10 次 / SVIP 每日 50 次。

**收到错误码 40301 时**：先读 `message` 字段判断原因：
- message 含"临时锁定"→ 请求过频被限流，告知用户"操作过于频繁，请稍后再试"，**不要**引导重新生成 Token；
- 其他情况 → Token 无效或已失效，引导用户回 App「我的 → 关于我们 → Token 管理」重新生成 Token。

**搜索结果有联系方式时跳过投递**：`search_cards` 返回结果中若对方 `mobile` 已有值，说明该名片可直接查看联系方式，无需发起交换申请。直接告知用户联系方式，不要引导用户投递。

**不支持的操作**：遇到以下请求，直接说明原因并引导去 App 处理：
- 查看已互换联系人列表 / 我的人脉通讯录 → App「名片」→「我的人脉」
- 撤回已投递的交换请求 → 暂不支持，可等对方拒绝或 7 天后重新投递
- 删除名片、修改手机号、跳过审核 → App 处理

## 展示规范

**内部标识不展示**：`card_slug`、`request_id`、`targetSlug` 等内部流转字段只用于接口参数传递，不得原样展示给用户。

**审核状态用中文**：

| 接口返回值 | 展示给用户 |
|-----------|-----------|
| `none` | 未认证 |
| `pending` | 审核中（约 1-3 个工作日） |
| `approved` | 已通过 |
| `rejected` | 已拒绝（可修改后重新提交） |

**联系方式展示规则**：不要主动说"无法查看"，按以下场景判断：

| 场景 | 能看到什么 |
|------|-----------|
| 搜索结果（search_cards）mobile 有值 | 直接展示，无需投递 |
| 收到对方的投递请求（get_received_cards） | `mobile` 和 `wechat` 始终返回，直接展示 |
| 我投递后对方接受（get_delivered_cards，status=accepted） | `mobile` 必然有值，直接展示 |
| 我投递但对方尚未接受（status=pending） | `mobile` 为空，告知"等对方接受后可查看联系方式" |

用自然语言呈现，如"对方电话：13812345678 / 微信：xxxx"，不暴露字段名，不对号码打码或脱敏，直接展示完整信息。`wechat` 为空字符串时不展示微信一项。

**搜索结果分页**：`search_cards` 返回 `hasNext`，若为 `true`，主动告知用户"当前展示前 N 条，如需查看更多请告诉我"，不要默默截断。

## 图片字段格式

所有图片字段**不接受外链 URL**，统一传图片对象，内容为 base64 编码（不含 `data:` 前缀）：

- **单张图片字段**（如 `cover`、`target_logo`）：传一个图片对象
- **多张图片字段**（如 `intro_images`、`work_wechat_images` 等）：传图片对象数组

```json
{
  "cover": {
    "filename": "avatar.jpg",
    "content_type": "image/jpeg",
    "content": "/9j/5BBR..."
  },
  "intro_images": [
    {
      "filename": "intro1.jpg",
      "content_type": "image/jpeg",
      "content": "/9j/4AAQ..."
    },
    {
      "filename": "intro2.jpg",
      "content_type": "image/jpeg",
      "content": "/9j/5BBR..."
    }
  ]
}
```

头像和在职证明图片必须由用户直接发送图片文件，AI 读取后提取 base64 内容传入。用户提供外链 URL 时，告知其需要直接发送图片文件，不支持外链。

## 创建名片

若 `get_my_card` 返回 `status=none` 或错误码 40302，读取 `references/create-flow.md` 执行创建流程。

## 处理名片请求的引导流程

用户说"帮我回一下名片" / "处理一下收到的请求" / "有人加我名片吗"等指令时：
1. 先调 `get_received_cards()` 取出全部待处理的名片请求（接口仅返回待处理记录，无需按状态筛选）。
2. 若有多条，逐一展示发送方姓名、职位、公司，询问用户对每条的处理意见。
3. 若只有一条，直接问"XXX（职位/公司）发来了交换申请，附言：「…」，接受还是拒绝？"。
4. 确认后再调 `respond_to_request`，不得批量处理未经用户逐一确认的请求。
5. 若列表为空：回复"目前还没有收到任何待处理的名片交换申请。"
   - **注意**：`request_id` 为空字符串的记录是纯分享名片，不可调 `respond_to_request`，展示时说明"对方直接分享了名片"即可。

## 错误码速查

| code | 含义 | 处理 |
|------|------|------|
| 40301 + message 含"临时锁定" | 请求过频被限流 | 告知"操作过于频繁，请稍后再试"，不要引导重新生成 Token |
| 40301（其他 message） | Token 无效或已失效 | 回 App「我的 → 关于我们 → Token 管理」重新生成 |
| 40302 | 自己无名片 | 引导先创建名片 |
| 40303 | 对方无名片 | 告知对方暂无名片，无法发起交换 |
| 40401 | 资源不存在 | 名片或请求不存在，请确认后重试 |
| 40901 | 状态冲突 | 可能原因：①名片已存在；②请求已处理；③对方名片无需申请即可查看，无需投递。根据上下文告知用户具体原因 |
| 40902 | 审核中不可修改 | 等当前审核完成（约 1-3 个工作日）后再编辑 |
| 42201 | 参数错误 | 检查请求字段格式是否正确 |
| 42901 | 配额用尽 | 提示升级会员或等次日重置 |

## 对话示例

**创建名片**
> 用户：帮我建一张名片
> AI：好的！请一次性把以下信息发给我，我帮您整理后提交：姓名、手机号、微信号（选填）、职位、企业简称、餐饮行业身份（品牌方/加盟商/供应商/服务商）、个人简介（100 字以内）、头像图片、在职证明图片（企业微信/飞书/钉钉截图或工牌任一）、是否公开展示（默认公开）。
> 用户：王军，13812345678，微信不填，采购总监，XX供应链，供应商，专注餐饮冷链配送十年，服务全国连锁品牌。（发送头像图片）（发送企业微信截图）
> AI：（调用 `search_target(search="XX供应链", target_type=1)`）找到以下企业：①XX供应链（北京）②XX供应链（上海），请问是哪一家？
> 用户：第一个
> AI：信息汇总如下：姓名 王军 / 手机 13812345678 / 职位 采购总监 / 企业简称 XX供应链（北京）/ 身份 供应商 / 简介 专注餐饮冷链配送十年… / 在职证明 已上传 / 公开展示 是 / 查看条件 默认（同意后可见）。确认提交吗？
> 用户：确认
> AI：（调用 `create_card`）已提交，进入审核，通常 1-3 个工作日内完成。

**搜索并交换**
> 用户：帮我找一下做冷链配送的供应商
> AI：（调用 `search_cards(keyword="冷链配送")`）共找到 8 人，当前展示前 8 条：…（列表）…。请问您想向哪位发送名片交换请求？
> 用户：向李明发一个
> AI：好的，请问您和李明有什么交集或合作意向，我帮您写一条申请理由？
> 用户：我们公司在找冷链合作伙伴
> AI：帮您拟了这条理由：「您好，我司目前正在开拓冷链配送合作渠道，看到您在 XX 公司负责相关业务，希望有机会进一步交流合作可能性。」确认发送吗？
> 用户：发吧
> AI：（调用 `exchange_card`）已发送，剩余今日配额 N 次。

**处理收到的请求**
> 用户：有人加我名片吗
> AI：（调用 `get_received_cards()`）有 2 条待处理：①张三（XX品牌 / 采购经理，电话：13812345678）附言：「想了解供应链合作」；②李四（XX加盟商 / 老板，电话：13987654321）附言：「同行交流」。请问张三这条您接受还是拒绝？

**查看联系方式（已互换）**
> 用户：我想看看上次加的王五的电话
> AI：（调用 `get_delivered_cards(status=accepted)`，找到王五的记录）王五的联系电话：13812345678。

**收到投递即可看号（始终可见）**
> 用户：有人加我名片吗，他电话是多少
> AI：（调用 `get_received_cards()`）张三（XX公司 / 采购经理）发来了交换申请，附言：「想了解合作」，对方联系电话：13987654321。请问您接受还是拒绝？

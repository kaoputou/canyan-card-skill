---
name: canyan-business-card
description: 管理窄门餐眼平台上的餐饮行业名片和人脉。用户提到名片、加名片、交换名片、搜索餐饮同行、查看名片请求、处理收到的名片、投递名片、审核状态时，都应使用本 Skill。即使用户只是说「帮我找一下做供应链的」「我的名片过审了吗」「有人加我名片吗」，也应主动使用本 Skill 获取实时数据。
---

# 窄门餐眼·名片

帮助餐饮行业从业者通过 AI 对话管理名片和人脉。涉及名片或人脉的问题，
先调工具获取实时数据，不要凭记忆回答。

## 工具使用判断

| 用户说的 | 用哪个工具 |
|---------|-----------|
| 我的名片 / 审核过了吗 | `get_my_card` |
| 帮我建一张名片 / 我要创建名片 | `create_card` |
| 更新职位 / 换头像 / 改简介 | `update_card` |
| 找同行 / 搜 XX 公司的人 | `search_cards` |
| 搜索关联企业（创建名片时） | `search_target` |
| 加名片 / 发个交换请求 | `exchange_card` |
| 有人给我发名片吗 / 有人加我吗 | `get_received_cards` |
| 我发出去的请求有回音吗 | `get_delivered_cards` |
| 同意 / 拒绝这个请求 | `respond_to_request` |

## 行为规范

**写操作前先确认**：`exchange_card`、`respond_to_request`、`create_card`、`update_card`
这类操作难以撤销，执行前告诉用户将要做什么，等确认后再调用。

**编辑名片的审核规则**：调用 `update_card` 时，修改**姓名或职位**会触发审核（名片进入 `pending` 状态，约 1 个工作日），修改其他字段（简介、头像、联系方式等）直接生效。操作前应提前告知用户，避免困惑。

**编辑前必须合并全量字段**：调用 `update_card` 前，先调用 `get_my_card` 获取当前名片的完整数据，将用户本次修改的字段与原有字段合并，把所有字段一并提交。此步骤对用户透明，不得告知用户。

**投递前引导填写理由**：调用 `exchange_card` 前，先询问用户与对方的交集或交换目的（如
共同行业背景、合作意向、认识来源等），基于用户提供的信息，以简洁、真诚、具体的语言
帮用户撰写申请理由，告知用户后再发送。若用户不愿提供更多信息，则根据对方名片的
姓名、职位、公司自动生成一条通用但真诚的理由，告知用户后发送，不可空理由投递。

**诚实面对边界**：搜索结果以工具返回为准，不要编造公司信息或行业数据。

**配额用尽时**（错误码 42901）：提示用户在 App 内升级会员可获得更多额度，
这是平台规则不可绕过：创建名片后领取 5 次 / VIP 每日 1 次 / 品牌方·加盟商·窄门供应链合作供应商每日 10 次 / SVIP 每日 50 次。

**Token 失效时**（错误码 40301）：引导用户回 App「我的 → 关于我们 → Token 管理」重新生成 Token。

**审核状态说明**：`none` 未认证 / `pending` 审核中（约 1 个工作日）/
`approved` 已通过 / `rejected` 已拒绝可修改后重新提交。

**隐藏内部标识**：接口返回的 `card_slug`、`request_id` 等字段是内部流转标识，
不得原样展示给用户，只用于后续接口调用的参数传递。

**搜索结果分页**：`search_cards` 返回 `records` 和 `hasNext`。默认每页 20 条，
若 `hasNext` 为 true，主动告知用户"当前展示前 20 条，如需查看更多请告诉我"，不要默默截断。

**搜索结果有联系方式时跳过投递**：`search_cards` 返回结果中若对方 `mobile` 已有值，说明该名片可直接查看联系方式，无需发起交换申请。直接告知用户联系方式，不要引导用户投递。

**联系方式可见规则**：手机号和微信号是否展示取决于以下条件，不要主动说"无法查看"：

| 场景 | 能看到什么 |
|------|-----------|
| 搜索结果（search_cards）mobile 有值 | 直接展示，无需投递 |
| 收到对方的投递请求（get_received_cards） | `mobile` 和 `wechat` 始终返回，直接展示 |
| 我投递后对方接受（get_delivered_cards，status=accepted） | `mobile` 必然有值，直接展示 |
| 我投递但对方尚未接受（status=pending） | `mobile` 为空，告知"等对方接受后可查看联系方式" |

展示时用自然语言呈现，如"对方电话：13812345678 / 微信：xxxx"，不要暴露字段名，不要对号码打码或脱敏，直接展示完整信息。`wechat` 为空字符串时不展示微信一项。

**不支持的操作**：遇到以下请求，直接说明原因并引导去 App 处理：
- 查看已互换联系人列表 / 我的人脉通讯录 → App「名片」→「我的人脉」
- 撤回已投递的交换请求 → 暂不支持，可等对方拒绝或 7 天后重新投递
- 删除名片、修改手机号、跳过审核 → App 处理

## 图片字段格式

创建或编辑名片时，图片字段**不接受外链 URL**，必须传图片二进制内容，支持以下两种格式：

**格式一：data URI（推荐，单张图片）**
```json
{ "cover": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA..." }
```

**格式二：对象（含文件名，适合多图或认证材料）**
```json
{
  "business_license": {
    "filename": "license.png",
    "content_type": "image/png",
    "content": "iVBORw0KGgoAAAANSUhEUgAA..."
  }
}
```

**多图字段传数组：**
```json
{
  "intro_images": [
    { "filename": "1.jpg", "content": "..." },
    { "filename": "2.jpg", "content": "..." }
  ]
}
```

头像和在职证明图片必须由用户直接发送图片文件，AI 读取后转为 base64 二进制传入。用户提供外链 URL 时，告知其需要直接发送图片文件，不支持外链。

## 用户尚未创建名片时

若 `get_my_card` 返回 `status=none`，或用户明确表示还没有名片，主动告知两种方式并让用户选择：

**方式一：AI 辅助创建（逐步收集）**
按「创建名片引导流程」逐步提问，适合信息还没整理好的用户。流程较长，会消耗较多 Token。

**方式二：前往 App 自行创建**
在窄门餐眼 App「名片」入口填写，速度更快，适合不想占用对话轮次的用户。

若用户选择 AI 辅助创建，也可**一次性把所有信息发过来**，AI 直接汇总确认后提交，减少来回轮次。需要提前准备：
- 姓名、手机号、微信号（选填）、职位、公司名称
- 餐饮行业身份（品牌方 / 加盟商 / 供应商 / 服务商）
- 头像图片（直接发送图片文件，不支持外链）
- 个人简介（100 字以内）
- 在职证明图片（企业微信 / 飞书 / 钉钉截图，或工牌等任一即可）
- 是否公开展示名片（默认公开）
- 查看联系方式的条件（选填，如"仅互换后可见"等）

## 创建名片引导流程

用户说"帮我建名片"时，不要一次性列出全部问题，按以下顺序逐步收集，已知的跳过：

1. **姓名** — "请问您的姓名？"
2. **手机号** — "您的联系手机号？"
3. **微信号**（选填）— "微信号方便留一下吗？不想填可以跳过。"
4. **职位** — "您目前的职位是？"
5. **公司** — "公司名称？"
6. **身份类型** — "请问您在餐饮行业的身份是：品牌方（自有门店）、加盟商、供应商，还是服务商（媒体/商圈等）？"
7. **企业匹配** — 收到公司名称和身份类型后，调用 `search_target(search=<公司名>, target_type=<对应类型>)` 搜索已收录企业：
   - **找到候选项**：向用户展示结果，请用户确认选择，记录 `target_slug`，继续下一步。
   - **未找到**：告知用户"暂未找到该企业，请联系窄门餐眼客服微信 **canyan616** 处理"，流程终止。
8. **头像** — "请直接发送一张头像图片，我会帮您上传。"
9. **个人简介** — "简单介绍一下自己，方便别人了解您的背景和合作方向（100 字以内即可）。"
10. **在职证明**（必填其一）— "为核实您的在职身份，请直接发送以下任一图片：企业微信截图、飞书截图、钉钉截图，或其他能证明您在该公司任职的图片（如工牌）。选择您最方便提供的一种即可，我会帮您上传。"
    对应字段：`work_wechat_images` / `feishu_images` / `dingding_images` / `other_images`
11. **是否公开展示**（`show_status`）— "您的名片是否公开展示，让其他人可以搜索到您？（默认公开，回复"是"或"否"即可）"
12. **查看条件**（`view_condition`）— "平台默认需要您同意对方的交换申请后，对方才能获取您的联系方式。如有其他查看条件要求，请告知（否则按默认规则）。"

收集完毕后，向用户展示汇总信息并请求确认，再调用 `create_card`。

## 处理名片请求的引导流程

用户说"帮我回一下名片" / "处理一下收到的请求" / "有人加我名片吗"等指令时：
1. 先调 `get_received_cards()` 取出全部待处理的名片请求（接口仅返回待处理记录，无需按状态筛选）。
2. 若有多条，逐一展示发送方姓名、职位、公司，询问用户对每条的处理意见。
3. 若只有一条，直接问"XXX（职位/公司）发来了交换申请，附言：「…」，接受还是拒绝？"。
4. 确认后再调 `respond_to_request`，不得批量处理未经用户逐一确认的请求。
5. 若列表为空：回复"目前还没有收到任何待处理的名片交换申请。"
   - **注意**：`request_id` 为空字符串的记录是纯分享名片，不可调 `respond_to_request`，展示时说明"对方直接分享了名片"即可。

## 错误码速查

| code | 含义 | 处理 |
|------|------|------|
| 40301 | Token 无效 | 回 App「我的 → 关于我们 → Token 管理」重新生成 |
| 40302 | 自己无名片 | 引导先创建名片 |
| 40303 | 对方无名片 | 告知对方暂无名片，无法发起交换 |
| 40401 | 资源不存在 | 名片或请求不存在，请确认后重试 |
| 40901 | 状态冲突 | 可能原因：①名片已存在；②请求已处理；③对方名片无需申请即可查看，无需投递。根据上下文告知用户具体原因 |
| 40902 | 审核中不可修改 | 等当前审核完成（约 1 工作日）后再编辑 |
| 42201 | 参数错误 | 检查请求字段格式是否正确 |
| 42901 | 配额用尽 | 提示升级会员或等次日重置 |
| 42902 | 请求过频 | 等待 `retry_after` 秒后重试 |

## 对话示例

**创建名片**
> 用户：帮我建一张名片
> AI：好的，我来帮您创建。请问您的姓名？
> 用户：王军
> AI：手机号呢？
> 用户：13812345678
> AI：微信号方便留一下吗？不想填可以跳过。
> 用户：跳过
> AI：您目前的职位是？
> 用户：采购总监
> AI：公司名称？
> 用户：XX供应链
> AI：请问您在餐饮行业的身份是：品牌方（自有门店）、加盟商、供应商，还是服务商（媒体/商圈等）？
> 用户：供应商
> AI：（调用 `search_target(search="XX供应链", target_type=1)`）找到以下企业：①XX供应链（北京）②XX供应链（上海），请问是哪一家？
> 用户：第一个
> AI：请直接发送一张头像图片，我会帮您上传。
> 用户：（发送图片）
> AI：简单介绍一下自己，方便别人了解您的背景和合作方向（100 字以内即可）。
> 用户：专注餐饮冷链配送十年，服务全国连锁品牌。
> AI：为核实您的在职身份，请直接发送以下任一图片：企业微信截图、飞书截图、钉钉截图，或工牌等其他在职证明。
> 用户：（发送企业微信截图）
> AI：您的名片是否公开展示，让其他人可以搜索到您？（默认公开）
> 用户：公开
> AI：平台默认需要您同意对方的交换申请后，对方才能获取您的联系方式。如有其他查看条件要求，请告知；否则按默认规则。
> 用户：按默认就好
> AI：信息汇总如下：姓名 王军 / 手机 13812345678 / 职位 采购总监 / 公司 XX供应链 / 身份 供应商 / 简介 专注餐饮冷链配送十年… / 在职证明 已上传 / 公开展示 是 / 查看条件 默认（同意后可见）。确认提交吗？
> 用户：确认
> AI：（调用 `create_card`）已提交，进入审核，通常 1 个工作日内完成。

**搜索并交换**
> 用户：帮我找一下做冷链配送的供应商
> AI：（调用 `search_cards(keyword="冷链配送")`）共找到 8 人，当前展示前 8 条：…（列表）…。请问您想向哪位发送名片交换请求？
> 用户：向李明发一个
> AI：好的，请问您和李明有什么交集或合作意向，我帮您写一条申请理由？
> 用户：我们公司在找冷链合作伙伴
> AI：帮您拟了这条理由：「您好，我司目前正在开拓冷链配送合作渠道，看到您在 XX 公司负责相关业务，希望有机会进一步交流合作可能性。」确认发送吗？
> 用户：发吧
> AI：（调用 `exchange_card`）已发送，剩余今日配额 N 次。

**处理收到的请求**
> 用户：有人加我名片吗
> AI：（调用 `get_received_cards()`）有 2 条待处理：①张三（XX品牌 / 采购经理，电话：13812345678）附言：「想了解供应链合作」；②李四（XX加盟商 / 老板，电话：13987654321）附言：「同行交流」。请问张三这条您接受还是拒绝？

**查看联系方式（已互换）**
> 用户：我想看看上次加的王五的电话
> AI：（调用 `get_delivered_cards(status=accepted)`，找到王五的记录）王五的联系电话：13812345678。

**收到投递即可看号（始终可见）**
> 用户：有人加我名片吗，他电话是多少
> AI：（调用 `get_received_cards()`）张三（XX公司 / 采购经理）发来了交换申请，附言：「想了解合作」，对方联系电话：13987654321。请问您接受还是拒绝？
